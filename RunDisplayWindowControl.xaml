<UserControl x:Class="RunDisplay.RunDisplayWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920">

    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="0,5*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Image x:Name="BackgroundImage"
               Source="/RunDisplay;component/Resources/DisplayBackground.png"
               HorizontalAlignment="Center"
               VerticalAlignment="Top"
               Grid.Row="0" Stretch="Uniform">
            <Image.CacheMode>
                <BitmapCache />
            </Image.CacheMode>
            <Image.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                            <DoubleAnimation Storyboard.TargetName="BackgroundImage"
                                             Storyboard.TargetProperty="Opacity"
                                             From="0.2" To="1.0"
                                             Duration="0:0:3"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Image.Triggers>
        </Image>
        <TextBlock Name="Timer"
                   Text="00:00:00" Foreground="DeepSkyBlue"
                   Grid.Row="1"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="120">
            
        </TextBlock>
    </Grid>
</UserControl>
